#include<iostream>
using namespace std;
int main(){
    int i=1,n;
    cin>> n;

 while(i<=n)
 if((i%n)==0) ++i;
 cout<<i<<endl;
 system("pause");
 return 0;
}
¿cual es la salida si se entroduce como valor de n.0?
la salida es:1
¿cual es la salida si se entroduce como valor de n.1?
la salida es:2
¿cual es la salida si se entroduce como valor de n.3?
la salida es: infinito que  nunca podria  terminar los numeros